// These variables come from building the frontend environment.
// Remember to update both in tandem so keep them in sync :)

export const API_URL = 'http://localhost:3010/api';
export const WS_URL = 'ws://localhost:3010';
export const ALLOW_DEBUG = true;
